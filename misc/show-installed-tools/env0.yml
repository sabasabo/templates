version: 1

deploy:
  steps:
    terraformPlan:
      before:
        - ./validate-tools-permissions.sh
        - node --version
        - npm --version
        - yarn --version
        - git --version
        - python2 --version
        - python3 --version
        - pip2 --version
        - pip3 --version
        - aws --version
        - gcloud --version
        - az --version
        - kubectl version --client
        - tfenv --version
        - terraform --version
        - jq --version
        - pwsh -v
        - touch /opt/test
        - rm /opt/test
        -touch /tmp/test
        - rm /tmp/test
        - touch /tmp5/test
        - rm /tmp5/test
